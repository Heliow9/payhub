# PayHub Android

Wrapper Android nativo do mesmo portal responsivo/PWA. O login é único: Master/Analista por e-mail e Funcionário por CPF + PIN.

Abra `apps/android` no Android Studio (JDK 17) e gere o APK/Bundle. A URL de produção está em `MainActivity.kt` (`https://paayhub.duckdns.org`).
